# 2026-06-19: Lesson 2 — File I/O & Error Handling

**Covered:**
- File I/O: `open()`, `with` context managers, read/write/append modes
- Error handling: `try`/`except`/`else`/`finally`, common exception types
- `pathlib.Path` for modern path manipulation
- Structured data: `json` and `csv` modules
- Hands-on: log analyzer that reads, parses, and reports on log files

**Status:** Lesson created and available for teaching.
